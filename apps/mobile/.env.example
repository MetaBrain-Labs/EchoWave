# Development-only default. Production builds leave this unset and ask on first launch.
# Android devices must use the development computer's LAN address instead of localhost.
# Production EAS profiles set EXPO_PUBLIC_REQUIRE_SERVER_SELECTION=true themselves.
EXPO_PUBLIC_API_URL=http://localhost:3001
